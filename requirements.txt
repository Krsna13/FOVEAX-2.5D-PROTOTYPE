# FOVEAX Trail: Dependencies
# 
# CRITICAL HARDWARE NOTE FOR PYTORCH:
# The primary target hardware is NVIDIA RTX 5050 Laptop GPU (Blackwell GB207, compute capability sm_120).
# The standard cu126 build of PyTorch installs but has NO compiled sm_120 kernels, resulting in
# runtime failure: "no kernel image is available for execution on the device".
# PyTorch MUST be installed from the cu130 wheel index:
#
#   pip install torch==2.14.0+cu130 torchvision==0.29.0+cu130 --extra-index-url https://download.pytorch.org/whl/cu130

--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.14.0+cu130
torchvision==0.29.0+cu130

# Core Scientific Computing & 3D Geometry
numpy>=1.26.0,<3.0.0
scipy>=1.12.0
open3d>=0.18.0
opencv-python>=4.8.0
matplotlib>=3.8.0

# GUI Dashboard & Visualization
PyQt5>=5.15.10

# Utilities, Configs & Telemetry
PyYAML>=6.0.0
psutil>=5.9.0
tqdm>=4.66.0

# Testing
pytest>=8.0.0
